query GetPortfolioStocks($portfolioId: Int!) {
    getPortfolioStocks(portfolioId: $portfolioId) {
        id
        quantity
        averagePrice
        stock {
            id
            ticker
            name
        }
    }
}
