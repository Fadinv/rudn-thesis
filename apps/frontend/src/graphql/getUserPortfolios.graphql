query GetUserPortfolios($fromVersion: Int) {
    getUserPortfolios(fromVersion: $fromVersion) {
        items {
            id
            name
            createdAt
            deleted
            version
        }
        hasMoreData
        maxVersion
    }
}
